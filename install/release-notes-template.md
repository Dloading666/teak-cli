<details open>
<summary><b>🇨🇳 简体中文</b></summary>

- 修复:Windows 上 claude/codex 等 CLI 工具"找不到"的根因——改读注册表真实 PATH,覆盖 scoop/volta/nvm/Anthropic 原生安装等所有安装方式(不再靠猜目录)。
- 新增:修改记录面板显示本窗口所有提交(push 后不消失),可展开看每条 commit 的文件;关闭重开即清空。
- 优化:设置页"妙手"改名"快捷键"(gambit 已固定,页面聚焦快捷键)。
- 新增:顶部快捷开关显示模式(设置→快捷键)——图标+快捷键 / 仅图标 / 隐藏(纯键盘操作)。

</details>

<details>
<summary><b>🇹🇼 繁體中文</b></summary>

- 修復:Windows 上 claude/codex 等 CLI 工具「找不到」的根因——改讀登錄檔真實 PATH,覆蓋 scoop/volta/nvm/Anthropic 原生安裝等所有安裝方式(不再靠猜目錄)。
- 新增:修改記錄面板顯示本視窗所有提交(push 後不消失),可展開看每條 commit 的檔案;關閉重開即清空。
- 優化:設定頁「妙手」改名「快捷鍵」(gambit 已固定,頁面聚焦快捷鍵)。
- 新增:頂部快捷開關顯示模式(設定→快捷鍵)——圖示+快捷鍵 / 僅圖示 / 隱藏(純鍵盤操作)。

</details>

<details>
<summary><b>🇬🇧 English</b></summary>

- Fixed: root cause of "claude/codex not found" on Windows — reads the real PATH from the registry, covering all install methods (scoop/volta/nvm/Anthropic native installer) instead of guessing directories.
- New: Changes panel shows all commits made this session (persist after push), expandable to per-commit files; resets on app close.
- Refined: Settings tab "妙手/Gambit" renamed to "Shortcuts" (gambit is now fixed, page focuses on hotkeys).
- New: Titlebar toggle display mode (Settings → Shortcuts): icon+shortcut / icon only / hidden (keyboard-only).

</details>

<details>
<summary><b>🇯🇵 日本語</b></summary>

- 修正:Windows で claude/codex などの CLI ツールが「見つからない」根本原因を修正 — レジストリから実際の PATH を読み取り、scoop/volta/nvm/Anthropic ネイティブインストールなど全インストール方式をカバー(ディレクトリ推測を廃止)。
- 新機能:変更記録パネルが現在のウィンドウの全コミットを表示(push 後も消えない)、コミットごとのファイルを展開表示;アプリ終了でリセット。
- 改善:設定ページの「妙手」を「ショートカット」に改名(gambit は固定化、ページはショートカットに焦点)。
- 新機能:タイトルバーのトグル表示モード(設定→ショートカット)——アイコン+ショートカット / アイコンのみ / 非表示(キーボードのみ)。

</details>

<details>
<summary><b>🇰🇷 한국어</b></summary>

- 수정:Windows에서 claude/codex 등 CLI 도구를 "찾을 수 없음"의 근본 원인 수정 — 레지스트리에서 실제 PATH를 읽어 scoop/volta/nvm/Anthropic 네이티브 설치 등 모든 설치 방식을 커버(디렉토리 추측 폐지).
- 신규:수정 기록 패널이 이번 세션의 모든 커밋 표시(push 후에도 사라지지 않음), 커밋별 파일 전개; 앱 종료 시 리셋.
- 개선:설정 페이지 "妙手"를 "단축키"로 개명(gambit 고정화, 페이지는 단축키에 초점).
- 신규:타이틀바 토글 표시 모드(설정→단축키) —— 아이콘+단축키 / 아이콘만 / 숨김(키보드만).

</details>
