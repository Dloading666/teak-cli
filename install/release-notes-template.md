<details open>
<summary><b>🇨🇳 简体中文</b></summary>

- 修复:Codex(Codex Desktop / CLI)子 Agent 会话不再以重复的顶层卡片堆积在历史列表--只显示用户创建的主会话。
- 修复:Codex Desktop 附带文件发送消息时,历史标题改为用户真实首问(不再是 `# Files mentioned by the user: ...`)。
- 修复:提示词卡片在最小高度时,右侧操作按钮不再被挤压压缩。
- 修复:任务列表三种形态(待办/便签/提示词)的空状态问候语统一--提示词视图也显示问候语,且三者高度对齐。

</details>

<details>
<summary><b>🇹🇼 繁體中文</b></summary>

- 修復:Codex(Codex Desktop / CLI)子 Agent 會話不再以重複的頂層卡片堆積在歷史列表--只顯示使用者建立的主會話。
- 修復:Codex Desktop 附帶檔案傳送訊息時,歷史標題改為使用者真實首問(不再是 `# Files mentioned by the user: ...`)。
- 修復:提示詞卡片在最小高度時,右側操作按鈕不再被擠壓壓縮。
- 修復:任務列表三種形態(待辦/便簽/提示詞)的空狀態問候語統一--提示詞視圖也顯示問候語,且三者高度對齊。

</details>

<details>
<summary><b>🇬🇧 English</b></summary>

- Fixed: Codex (Codex Desktop / CLI) sub-agent rollouts no longer pile up as duplicate top-level cards in the history list - only user-created main sessions are shown.
- Fixed: When Codex Desktop sends a message with attached files, the history title now shows the user's actual first question (instead of `# Files mentioned by the user: ...`).
- Fixed: Prompt-card action buttons no longer compress when the card is at its minimum height.
- Fixed: The empty-state greeting is now consistent across all three task-list forms (to-do / note / prompt) - the prompt view shows the greeting too, and all three align at the same height.

</details>

<details>
<summary><b>🇯🇵 日本語</b></summary>

- 修正:Codex(Codex Desktop / CLI)のサブエージェントセッションが、履歴リストに重複するトップレベルカードとして堆積しないようになりました--ユーザーが作成したメインセッションのみ表示。
- 修正:Codex Desktop で添付ファイル付きメッセージを送信した際、履歴タイトルがユーザーの実際の最初の質問を表示するようになりました(`# Files mentioned by the user: ...` ではなく)。
- 修正:プロンプトカードが最小高さのとき、右側のアクションボタンが圧縮されなくなりました。
- 修正:タスクリストの3つの形態(ToDo/メモ/プロンプト)の空状態の挨拶が統一されました--プロンプトビューでも挨拶が表示され、3つとも同じ高さに揃います。

</details>

<details>
<summary><b>🇰🇷 한국어</b></summary>

- 수정:Codex(Codex Desktop / CLI) 서브에이전트 세션이 히스토리 목록에 중복 최상위 카드로 쌓이지 않습니다. 이제 사용자가 만든 메인 세션만 표시됩니다.
- 수정:Codex Desktop에서 파일을 첨부해 메시지를 보낼 때 히스토리 제목이 사용자의 실제 첫 질문을 표시합니다(`# Files mentioned by the user: ...` 대신).
- 수정:프롬프트 카드가 최소 높이일 때 우측 액션 버튼이 더 이상 압축되지 않습니다.
- 수정:작업 목록 세 가지 형태(ToDo/메모/프롬프트)의 빈 상태 인사가 통일되었습니다--프롬프트 보기에서도 인사가 표시되고 세 가지 모두 같은 높이에 정렬됩니다.

</details>
