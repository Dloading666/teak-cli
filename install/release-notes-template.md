<details open>
<summary><b>🇨🇳 简体中文</b></summary>

- ✨ 新增外部启动:`coffee-cli launch --tool <工具> [--cwd <目录>]`,可被启动器、右键菜单、脚本直接调用,一打开就跳到指定工具 + 工作目录的会话标签页。已运行的实例不重启,新请求会转发到当前窗口。
- 🦞 修复 OpenClaw 会话记录:图标正常显示龙虾标志、名称显示「OpenClaw」(不再是无图灰块 / "workspace");重复会话已去重,删不掉的问题已解决。
- ✏️ 会话重命名打磨:进入编辑态时无边框、无布局位移,体验更顺滑。
- 🖥️ macOS 标题栏:红绿灯按钮居中、品牌内边距加宽(#102)。

</details>

<details>
<summary><b>🇹🇼 繁體中文</b></summary>

- ✨ 新增外部啟動:`coffee-cli launch --tool <工具> [--cwd <目錄>]`,可被啟動器、右鍵選單、腳本直接呼叫,一開啟就跳到指定工具 + 工作目錄的工作階段分頁。已執行的執行個體不重啟,新請求會轉發到目前視窗。
- 🦞 修復 OpenClaw 會話記錄:圖示正常顯示龍蝦標誌、名稱顯示「OpenClaw」(不再是無圖灰塊 / "workspace");重複會話已去重,刪不掉的問題已解決。
- ✏️ 會話重新命名打磨:進入編輯態時無邊框、無版面位移,體驗更順暢。
- 🖥️ macOS 標題列:紅綠燈按鈕置中、品牌內邊距加寬(#102)。

</details>

<details>
<summary><b>🇬🇧 English</b></summary>

- ✨ External launch: `coffee-cli launch --tool <tool> [--cwd <dir>]` — callable from launchers, context menus, and scripts; opens straight into an agent tab for the chosen tool + working directory. A running instance is never restarted — new requests forward to the live window.
- 🦞 Fixed OpenClaw session history: the lobster mascot now renders correctly and the row reads "OpenClaw" (no more grey placeholder / "workspace"); duplicate sessions are deduped and the "can't delete" issue is resolved.
- ✏️ Rename polish: switching into edit mode causes no border and zero layout shift.
- 🖥️ macOS title bar: traffic-light buttons centered, brand padding widened (#102).

</details>

<details>
<summary><b>🇯🇵 日本語</b></summary>

- ✨ 外部起動を追加:`coffee-cli launch --tool <ツール> [--cwd <ディレクトリ>]` — ランチャー、コンテキストメニュー、スクリプトから直接呼び出せ、指定ツール + 作業ディレクトリのセッションタブを一気に開きます。起動中のインスタンスは再起動せず、新しいリクエストは実行中のウィンドウへ転送されます。
- 🦞 OpenClaw の会話履歴を修正:ロブスターのアイコンが正しく表示され、行は「OpenClaw」と表示されます(もうグレーのプレースホルダー / "workspace" にはなりません)。重複セッションは統合され、削除できない問題も解決しました。
- ✏️ リネームの改良:編集モードへの切替時に境界線もレイアウトのズレもありません。
- 🖥️ macOS タイトルバー:トラフィックライトボタンを中央寄せ、ブランドの余白を拡大(#102)。

</details>

<details>
<summary><b>🇰🇷 한국어</b></summary>

- ✨ 외부 실행 추가:`coffee-cli launch --tool <도구> [--cwd <디렉터리>]` — 런처, 컨텍스트 메뉴, 스크립트에서 직접 호출해 지정한 도구 + 작업 디렉터리의 세션 탭으로 바로 진입합니다. 이미 실행 중인 인스턴스는 재시작되지 않으며, 새 요청은 실행 중인 창으로 전달됩니다.
- 🦞 OpenClaw 세션 기록 수정:랍스터 마스코트가 정상 표시되고 행은「OpenClaw」로 표시됩니다(더 이상 회색 자리표시 / "workspace" 아님). 중복 세션은 통합되고 삭제되지 않던 문제도 해결되었습니다.
- ✏️ 이름 변경 다듬기:편집 모드 진입 시 테두리도 레이아웃 이동도 없습니다.
- 🖥️ macOS 타이틀바:신호등 버튼 중앙 정렬, 브랜드 여백 확장(#102).

</details>
